#ifndef _BUILD_H_
#define _BUILD_H_

#include <memory>
#include "camera.h"
#include "hdr.h"
#include "loader.h"
#include "random.h"
#include "scene.h"
#include "tracer.h"
#include "utility.h"

bool build_1(Scene& scene);
bool build_2(Scene& scene);
bool build_3(Scene& scene);
bool build_4(Scene& scene);

#endif
